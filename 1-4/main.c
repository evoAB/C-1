#include <stdio.h>
#include <stdlib.h>

int main()
{
    int r;
    printf("Enter the radius of circle : ");
    scanf("%d",&r);
    printf("Area of circle is %.2f having the radius %d",2*3.14*r,r);

    return 0;
}
